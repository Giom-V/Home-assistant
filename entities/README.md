# Entities

This folder is used to organize the configuration for different types of
entities in Home Assistant. Each subfolder corresponds to a specific domain of
entities, such as `sensors`, `switches`, and `lights`.

**Documentation:** <https://www.home-assistant.io/docs/configuration/entities/>
